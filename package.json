{
	"name": "my-app",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "bun run build && bun run preview",
		"build": "bun build src/index.ts --outdir public/dist",
		"preview": "bun public/index.html",
		"test": "playwright test"
	},
	"devDependencies": {
		"@playwright/test": "^1.57.0"
	},
	"type": "module"
}
